# CHANGELOG (dev)

## Unreleased

- Инициализация dev changelog; добавлен шаблон окружения `.env.example`, CI workflow (Postgres + миграции + smoke pytest), фича-флаг `ENABLE_LEGACY_STATUS_API` (отключает старый POST /candidates/{id}/status в prod), unit-тесты на state-machine.
