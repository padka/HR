# Audit ND21 — Recruitsmart Admin Readiness (повторный)

Дата: 2025-11-18  
Исполнитель: Codex (автоматизированный аудит)

---

## 1. Краткий итог

- Проведён повторный технический аудит после доработок «ожидание слота» и ручного назначения собеседований. Функциональность промаркирована как beta: требуется доработка мониторинга и UX.
- Целевой `pytest` пакет: `python3 -m pytest tests/test_domain_repositories.py tests/test_manual_slot_assignment.py tests/test_intro_day_flow.py` → 11 passed (asyncio strict). Redis-интеграции по‑прежнему skipped локально.
- HR-флоу «ожидает слот» частично реализован: статус выставляется ботом, но отсутствуют SLA‑дашборды, bulk‑операции и автоматическое уведомление рекрутеров.
- UI/UX требует оптимизации: кнопка ручного назначения доступна только на detail‑экране, нет массовой работы со списком ожидающих, а форма не подсвечивает несогласованность часового пояса.
- Мониторинг и QA покрытие из ND21_TZ (health endpoints, Telegram sandbox) остаются невыполненными.

---

## 2. Среда и тесты

| Команда | Результат | Комментарий |
| --- | --- | --- |
| `python3 -m pytest tests/test_domain_repositories.py tests/test_manual_slot_assignment.py tests/test_intro_day_flow.py` | 11 passed | Проверены новые статусы, ручное назначение слота и поток ознакомительного дня |
| Redis | отсутствует локально | интеграционные тесты `tests/integration/test_notification_broker_redis.py` → `skip` |
| Миграции | не применялись | требуется запуск `python scripts/run_migrations.py` перед релизом |

---

## 3. Ключевые наблюдения

1. **Флоу назначения слотов**  
   - Бот переводит кандидата в `waiting_slot`, но UI отображает лишь бейдж, без фильтров/виджетов.  
   - Ручное назначение создаёт слот и мгновенно подтверждает его, однако нет проверки конфликта по таймзоне и отсутствует audit‑лог.  
   - Массовая работа с очередью не реализована, рекрутер должен открывать каждую карточку.

2. **Мониторинг и e2e**  
   - `/health/notifications` не репортит `poll_skipped_total`, `rate_limit_wait_total`, `intro_day_queue_depth`.  
   - Telegram sandbox и e2e логирование (ND21_TZ п.1.5/2.3) всё ещё TODO; ручных инструкций для QA нет.

3. **UI/UX**  
   - Кнопка «Предложить время» скрыта для статусов, отличных от `waiting_slot`, хотя ручное назначение может понадобиться после фейлов авто-сущностей.  
   - Форма не проговаривает конечный часовой пояс и не фиксирует SLA (например, «сообщение кандидату отправлено»).  
   - Нет подсветки кандидатов, зависших >24ч без слота.

---

## 4. Техническое задание для разработчиков

### 4.1. Завершение флоу «ожидает слот»
1. **Slot availability API**:  
   - Экспортировать `city_has_available_slots`/`find_city_by_plain_name` в REST (`/api/cities/{id}/capacity`) для UI, кешировать на 5 мин.  
   - Добавить nightly cron (FastAPI background) для маркировки кандидатов `waiting_slot` → `stalled_waiting_slot` после 24ч без назначений.
2. **HR-доска**:  
   - В `candidates_list.html` добавить отдельную колонку «Ждут слота» с сортировкой по `status_changed_at`.  
   - Добавить фильтр «только waiting_slot» и счётчик на верхней панели.
3. **Рекрутерские уведомления**:  
   - При переводе в `waiting_slot` публиковать событие в Slack/Email (см. `backend/apps/admin_ui/services/notifications.py`) + асинхронный digest раз в час.

### 4.2. Ручное назначение собеседований
1. **Форма**:  
   - Перевести `schedule_manual_slot.html` на React компонент (reuse `form_shell`).  
   - Автоподстановка таймзоны + human readable preview (`Europe/Volgograd → GMT+04`).  
   - Завести чекбокс «отправить персональное сообщение» с textarea; при включении использовать шаблон `manual_slot_offer`.
2. **Back-end**:  
   - В `schedule_manual_candidate_slot` логировать операции (новая таблица `manual_slot_audit` с полями `candidate_id`, `recruiter_id`, `scheduled_by`, `payload`).  
   - Поддержать назначение в `waiting_slot` и `test1_completed`, не ограничивать одним статусом.  
   - Добавить проверку пересечений по recruiter_id/времени ±15 мин.

### 4.3. Health & мониторинг
1. `/health/notifications`: добавить поля `queue_depth`, `poll_skipped_total`, `waiting_slot_total`.  
2. Prometheus exporter (`/metrics`): вынести метрики из health в отдельные counters.  
3. Документация: обновить `docs/NOTIFICATIONS_E2E.md` с описанием того, как читать новые метрики.

### 4.4. Telegram e2e sandbox
1. Реализовать sandbox (mock Telegram API) с фиксацией отправленных сообщений в SQLite (см. ND21_TZ п.1.5).  
2. Добавить pytest-метку `@pytest.mark.telegram_sandbox` и smoke-сценарий: Test1 → waiting_slot → manual slot → уведомление.  
3. В CI запускать sandbox-тесты nightly, публиковать артефакт `sandbox-report.json`.

### 4.5. DX и тесты
1. Включить Redis контейнер в `docker-compose.yml` для локального прогона интеграций.  
2. Добавить `make test-slot-flow` таргет (включает новые тесты).  
3. Удалить дублирующиеся проверки в `tests/test_intro_day_flow.py` (переиспользовать fixtures).

### 4.6. Представление «Ознакомительный день»
1. **Два pipeline'а на `/candidates`**  
   - `pipeline=interview` (дефолт) ↔ `pipeline=intro_day`. Переключатель в UI синхронизируется с query string, фильтры/пагинация сохраняют активную воронку.  
   - Back-end (`list_candidates`) возвращает `pipeline_meta`, `pipeline_options`, `summary.funnel` и `views.table.rows[].intro_day` для построения карточек.
2. **Funnel overview**  
   - Карточки стадий (⏳ очередь, 📆 приглашены, 👍 подтвердили, 🏁 результат, ⚠️ отказ) показывают количество, долю от списка и конверсию между стадиями.  
   - Список статусов каждой стадии рендерится из `summary.funnel[*].statuses`, поэтому QA может быстро сверить конфигурацию фильтров.
3. **Табличное представление**  
   - В режиме `intro_day` таблица показывает дату/время ОД, адрес, контакт, ответственного рекрутёра, быстрые действия «Назначить/Переназначить».  
   - Столбец «Дата и время» использует `fmt_local(.., intro_day.tz_name)` и подсвечивает отсутствующие назначения бейджем «Не назначено».  
   - Для интервальной воронки таблица не меняется, чтобы сохранить привычный UX.
4. **QA чек-лист**  
   - Переключить pipeline, убедиться, что URL содержит `pipeline=intro_day`, данные обновились, фильтры сохранились.  
   - Проверить карточки воронки: суммы стадий совпадают с числом строк, конверсии отображаются только для стадий с учётом tracking-флага.  
   - Таблица: статус «Ждёт назначения» отображает кнопку «Назначить ОД», для назначенных отображается слот и действие «Переназначить».

---

## 5. Задание для QA (функционал + UX)

### 5.1. Функциональное тестирование
1. **Waiting slot lifecycle**  
   - Шаги: пройти Test1 → убедиться, что статус = `Ждёт слота`, уведомление рекрутеру пришло.  
   - Проверить автоперевод в `stalled_waiting_slot` через mock времени.  
2. **Manual slot scheduling**  
   - Сценарии: разные часовые пояса, время в прошлом (ошибка), несвязанный город, повторное назначение.  
   - Проверить запись в `manual_slot_audit` и наличие события в UI (alert).  
3. **Health & metrics**  
   - Остановить брокер → `/health/notifications` должен отражать `poll_skipped_total > 0`, `waiting_slot_total` – актуальное.  
   - В Prometheus убедиться в наличии новых метрик и алертов.
4. **Telegram sandbox e2e**  
   - Прогнать сценарий Test1 → manual slot через UI → получить уведомление в sandbox, проверить payload (дата/время, кнопки).  
   - Смоделировать ошибку отправки и убедиться, что админка показывает предупреждение.

### 5.2. UX-проверка
1. **Candidates list board**  
   - Убедиться, что колонка «Ждут слота» видна, сортировка корректна, карточки содержат SLA‑бейдж (время ожидания).  
   - Проверить, что кнопка «Назначить время» доступна из карточки канбана (context menu).
2. **Manual slot form**  
   - Оценить читабельность подсказок/таймзоны, наличие подтверждения после отправки.  
   - Проверить адаптивность (ширина 320px) и клавиатурную навигацию.
3. **Accessibility**  
   - Контрасты кнопок `waiting_slot` бейджа ≥ WCAG AA.  
   - Все новые алерты и toasts должны иметь `role="status"`/`aria-live`.
4. **Docs onboarding**  
   - Пройти по инструкции в `docs/NOTIFICATIONS_E2E.md`: все шаги должны быть актуальны, ссылки рабочие, нет устаревших скриптов.

QA-результаты фиксировать в тест‑матрице с указанием: сценарий, шаги, ожидаемый/фактический результат, UI замечания (стробоскопия, незакрытые модалки и т.д.).

---

## 6. Оставшиеся риски
- **Мониторинг**: пока нет метрик для новых статусов – нельзя построить алерты на SLA ожидания.  
- **Conflict resolution**: ручное назначение может столкнуться с параллельным авто-подтверждением (нет optimistic lock).  
- **UX**: высокая плотность CTAs на карточке кандидата; требуется дополнительный UX‑проход после внедрения.

---

Документ обновлён: 2025‑11‑18. При закрытии задач обязательно обновить этот файл и `ND21_TZ.md` со ссылками на PR/релизные заметки.
