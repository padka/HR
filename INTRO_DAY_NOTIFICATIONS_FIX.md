# Исправление уведомлений ознакомительного дня

## Проблема
Уведомления при назначении ознакомительного дня не доходят до кандидатов.

## Корневая причина
NotificationService запускался только при `supervise_bot=True`, что не работало в некоторых конфигурациях.

## ✅ Исправления (уже применены)

### 1. NotificationService теперь запускается независимо

**Файл:** `backend/apps/admin_ui/state.py:375-382`

```python
# Start notification service (independent of bot supervision)
# Use poll loop if scheduler is not available or in development
allow_poll_loop = settings.environment != "production" or scheduler is None
notification_service.start(allow_poll_loop=allow_poll_loop)
logger.info(
    "Notification service started",
    extra={"allow_poll_loop": allow_poll_loop, "has_scheduler": scheduler is not None},
)
```

### 2. Timezone-aware dates для таблицы слотов

**Файл:** `backend/apps/admin_ui/services/slots.py:619`

```python
for item in items:
    if item.start_utc:
        item.start_utc = item.start_utc.replace(tzinfo=timezone.utc) if item.start_utc.tzinfo is None else item.start_utc
```

## 🔍 Диагностика

### Проверка системы уведомлений

Запустите диагностический скрипт:

```bash
ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/diagnose_notifications.py
```

**Ожидаемый результат:**
```
📊 Checking outbox notifications...
   Total outbox notifications: XX
   Pending: 0-5
   Sent: XX
   Failed: 0-2

📅 Checking intro_day slots...
   Total intro_day slots: XX

🤖 Checking bot configuration...
   ❌ Bot is NOT configured: Bot is not configured
   (это нормально при запуске отдельного скрипта)
```

### Проверка инициализации сервера

Запустите:

```bash
ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/diagnose_server.py
```

**Ожидаемый результат:**
```
🤖 Checking bot configuration...
   ✅ Bot IS configured!
      Username: @your_bot_username
      ID: 123456789

📬 Checking notification service...
   ✅ Notification service IS configured
      Running: True
```

## 🧪 Тестирование

### Запуск E2E тестов

```bash
ENVIRONMENT=development REDIS_URL="" .venv/bin/python -m pytest tests/test_intro_day_e2e.py -xvs
```

**Ожидаемый результат:**
```
tests/test_intro_day_e2e.py::test_intro_day_notification_end_to_end PASSED
tests/test_intro_day_e2e.py::test_notification_service_processes_intro_day PASSED
tests/test_intro_day_e2e.py::test_notification_broker_publishes_and_reads PASSED

============================== 3 passed
```

### Запуск всех тестов

```bash
make test-slot-flow
```

## 📝 Проверка в реальном окружении

### 1. Запустите сервер

```bash
make dev
```

### 2. Проверьте логи при старте

Должны быть такие записи:
```
[INFO] backend.apps.admin_ui.state: Notification service started
[INFO] backend.apps.admin_ui.state: Bot integration initialised
```

### 3. Назначьте ознакомительный день через UI

1. Откройте http://localhost:8000
2. Перейдите к кандидату
3. Нажмите "Предложить ознакомительный день"
4. Заполните форму и отправьте

### 4. Проверьте outbox

```bash
ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/diagnose_notifications.py
```

Должна быть запись:
```
📬 Pending notifications:
   ID: XX
      Type: intro_day_invitation
      Booking ID: XX
      Candidate TG ID: XXXXXXXXX
      Attempts: 0
```

### 5. Подождите 3-5 секунд

NotificationService poll'ит outbox каждые 3 секунды. Уведомление должно быть отправлено.

### 6. Проверьте что уведомление отправлено

Снова запустите:
```bash
ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/diagnose_notifications.py
```

Pending notifications должно быть 0, а Sent должно увеличиться.

## ❌ Возможные проблемы

### Проблема: Bot is not configured

**Причина:** BOT_TOKEN не установлен или неверный

**Решение:**
1. Проверьте `.env`:
   ```bash
   grep BOT_TOKEN .env
   ```

2. Убедитесь что токен правильный:
   ```bash
   ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/test_bot_init.py
   ```

### Проблема: Notification service not started

**Причина:** Ошибка при запуске NotificationService

**Решение:**
1. Проверьте логи сервера при старте
2. Убедитесь что REDIS_URL установлен (или оставьте пустым для InMemory режима)
3. Перезапустите сервер:
   ```bash
   pkill -f uvicorn
   make dev
   ```

### Проблема: Pending notifications не обрабатываются

**Причина:** NotificationService не poll'ит или не может отправить

**Решение:**
1. Проверьте что бот настроен (см. выше)
2. Проверьте логи сервера на ошибки
3. Проверьте failed notifications:
   ```bash
   ENVIRONMENT=development REDIS_URL="" .venv/bin/python scripts/diagnose_notifications.py
   ```

4. Если есть failed с last_error, исправьте ошибку

## 📊 Новая воронка ознакомительного дня

Текущая страница «Кандидаты» теперь поддерживает переключение между двумя представлениями:

1. **Интервью** — стандартная воронка (по умолчанию): новые → назначенные → подтверждённые → Test 2.  
2. **Ознакомительный день** — отдельное представление кандидатов, которые прошли Test 2:
   - ждут назначения ОД (`test2_completed`);
   - приглашены (`intro_day_scheduled`);
   - подтвердили участие (`intro_day_confirmed_preliminary`, `intro_day_confirmed_day_of`);
   - итог (закреплён/не закреплён или отказ).

### Как работает переключатель

- В верхней части страницы «Кандидаты» появился переключатель «Интервью / Ознакомительный день».  
- При выборе воронки:
  - фильтры автоматически ограничиваются релевантными статусами;
  - канбан, список, календарь и таблица показывают только кандидатов выбранного этапа;
  - метрики и «Ближайшие встречи» рассчитываются по слоту соответствующего типа (`purpose != intro_day` для интервью, `purpose == intro_day` для ОД).
- Все действия (фильтры, сортировки, пагинация) сохраняют активную воронку через параметр `pipeline`.

### QA чек-лист по новой воронке

1. **Переключатель** — при смене воронки сбрасывается страница на 1, фильтры сохраняются.  
2. **Список / Канбан / Календарь** — в режиме ОД отображаются только статусы из группы `intro_day_*`; интервальные слоты не попадают в календарь и наоборот.  
3. **Фильтры** — выпадающий список статусов содержит только статусы выбранной воронки; применение фильтра не сбрасывает режим.  
4. **URL** — при переключении добавляется `?pipeline=intro_day`, ручной ввод ссылки открывает нужное представление.  
5. **Доступность** — переключатель размечен `role="tablist"`, активное состояние объявляется скринридером.

## 🎯 Итоговый чеклист

- [x] NotificationService запускается независимо от bot supervision
- [x] Timezone-aware dates в таблице слотов
- [x] E2E тесты проходят
- [ ] Сервер запускается без ошибок (`make dev`)
- [ ] Бот настроен (`scripts/test_bot_init.py`)
- [ ] При создании intro_day слота создается outbox notification
- [ ] NotificationService обрабатывает notification за 3-5 секунд
- [ ] Уведомление приходит в Telegram

## 📚 Дополнительные ресурсы

- `MONITORING_QUICKSTART.md` - Быстрый старт мониторинга
- `docs/SERVER_STABILITY.md` - Детальная документация по стабильности
- `tests/test_intro_day_e2e.py` - E2E тесты уведомлений
- `scripts/diagnose_notifications.py` - Диагностика системы
- `scripts/diagnose_server.py` - Диагностика сервера
- `scripts/test_bot_init.py` - Проверка инициализации бота

## 🚀 Следующие шаги

1. ✅ Исправления уже применены в коде
2. Запустите сервер: `make dev`
3. Создайте intro_day слот через UI
4. Проверьте что уведомление пришло в Telegram
5. Если проблема осталась - следуйте разделу "Диагностика"

---

**Автор:** Claude Code
**Дата:** 2025-11-14
**Версия:** 1.0
