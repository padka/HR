# Автоматический парсинг username из Telegram

**Вопрос:** Как получить username кандидата без ручного ввода?
**Ответ:** Telegram автоматически предоставляет username в каждом сообщении! 🎉

---

## Как это работает

### 1. Telegram API автоматически предоставляет данные

Когда кандидат отправляет сообщение боту, Telegram **автоматически** включает полную информацию о пользователе:

```
┌────────────┐                    ┌─────────┐                    ┌────────┐
│  Кандидат  │                    │ Telegram│                    │  Бот   │
└─────┬──────┘                    └────┬────┘                    └───┬────┘
      │                                │                             │
      │  1. Отправляет /start          │                             │
      ├───────────────────────────────>│                             │
      │                                │                             │
      │                                │  2. Telegram добавляет      │
      │                                │     user data АВТОМАТИЧЕСКИ │
      │                                │                             │
      │                                │  message = {                │
      │                                │    from_user: {             │
      │                                │      id: 123456789,         │
      │                                │      username: "ivan_p",  ✅│
      │                                │      first_name: "Иван",    │
      │                                │      last_name: "П.",       │
      │                                │    },                       │
      │                                │    text: "/start"           │
      │                                │  }                          │
      │                                │                             │
      │                                │  3. Отправляет боту         │
      │                                ├────────────────────────────>│
      │                                │                             │
      │                                │                             │ 4. Бот парсит
      │                                │                             │    username
      │                                │                             │    автоматически
      │                                │                             │
      │                                │  5. Сохраняет в БД          │
      │                                │                             │
      │                                │     User.username = "ivan_p"│
      │                                │                             │
```

### 2. Код обработчика (уже реализовано!)

**Файл:** `backend/apps/bot/handlers/common.py`

```python
@router.message(Command("start"))
async def cmd_start(message: Message) -> None:
    user = message.from_user  # ✅ Telegram предоставляет автоматически

    user_id = getattr(user, "id", None)
    username = getattr(user, "username", None)  # ✅ Парсим автоматически!

    logger.info(
        "User started interview",
        extra={
            "user_id": user_id,
            "username": username,  # ✅ Записываем в лог
            "first_name": getattr(user, "first_name", None),
            "last_name": getattr(user, "last_name", None),
        }
    )

    # ✅ Передаем username дальше (НЕ спрашиваем у пользователя!)
    await services.begin_interview(user_id, username=username)
```

### 3. Сохранение в state

**Файл:** `backend/apps/bot/services.py`

```python
async def begin_interview(user_id: int, username: Optional[str] = None) -> None:
    state_manager = get_state_manager()
    await state_manager.set(
        user_id,
        State(
            flow="interview",
            username=username or "",  # ✅ Сохраняем автоматически полученный username
            # ... остальные поля ...
        ),
    )
```

### 4. Сохранение в БД

**Файл:** `backend/apps/bot/services.py` (при завершении Test 1)

```python
# После прохождения теста
username = state.get("username") or None  # ✅ Берем из state
candidate = await candidate_services.create_or_update_user(
    telegram_id=user_id,
    fio=fio,
    city=city_name,
    username=username,  # ✅ Сохраняем в БД
)
```

### 5. Использование в админке

**Файл:** `backend/apps/admin_ui/templates/candidates_detail.html`

```html
{% if user.username %}
  <!-- ✅ Username был получен автоматически! -->
  <a href="https://t.me/{{ user.username }}">Открыть чат</a>
{% else %}
  <!-- ⚠️ У пользователя нет @username в Telegram -->
  <span>(нет username)</span>
{% endif %}
```

---

## Полный Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 1: Кандидат начинает диалог                               │
└─────────────────────────────────────────────────────────────────┘
  Кандидат: /start
           ↓
  Telegram: Автоматически добавляет user data к сообщению
           ↓
  message.from_user = {
    id: 123456789,
    username: "ivan_petrov",  ← ✅ Telegram предоставляет автоматически!
    first_name: "Иван",
    last_name: "Петров",
  }

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 2: Бот обрабатывает команду /start                        │
└─────────────────────────────────────────────────────────────────┘
  handlers/common.py:
    username = getattr(user, "username", None)  ← ✅ Парсим!
    await services.begin_interview(user_id, username=username)

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 3: Сохранение в State                                     │
└─────────────────────────────────────────────────────────────────┘
  services.py:
    State(
      flow="interview",
      username=username or "",  ← ✅ Сохраняем
      ...
    )

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 4: Кандидат проходит Тест 1                               │
└─────────────────────────────────────────────────────────────────┘
  Кандидат отвечает на вопросы...

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 5: Сохранение в БД после завершения теста                 │
└─────────────────────────────────────────────────────────────────┘
  services.py (finalize_test1):
    username = state.get("username") or None
    await create_or_update_user(
      telegram_id=user_id,
      username=username,  ← ✅ Записываем в БД
    )

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 6: Рекрутер открывает профиль в админке                   │
└─────────────────────────────────────────────────────────────────┘
  candidates_detail.html:
    {% if user.username %}
      <a href="https://t.me/{{ user.username }}">Открыть чат</a>
                         ^^^^^^^^^^^^^^^^^^^^
                         ✅ Username из БД (получен автоматически!)
    {% endif %}

┌─────────────────────────────────────────────────────────────────┐
│ ЭТАП 7: Клик открывает личный чат                              │
└─────────────────────────────────────────────────────────────────┘
  https://t.me/ivan_petrov
           ↓
  Telegram Desktop/Web/Mobile открывает личный чат с кандидатом ✅
```

---

## Какие данные доступны автоматически

Telegram Bot API предоставляет следующие поля в **каждом сообщении**:

| Поле            | Тип          | Обязательно | Описание                              |
|-----------------|--------------|-------------|---------------------------------------|
| `id`            | `int`        | ✅ Да       | Telegram ID пользователя              |
| `is_bot`        | `bool`       | ✅ Да       | Является ли ботом                     |
| `first_name`    | `str`        | ✅ Да       | Имя                                   |
| `last_name`     | `str`        | ❌ Нет      | Фамилия                               |
| `username`      | `str`        | ❌ Нет      | @username (например, "ivan_petrov")   |
| `language_code` | `str`        | ❌ Нет      | Язык интерфейса ("ru", "en", etc.)    |
| `is_premium`    | `bool`       | ❌ Нет      | Есть ли Telegram Premium              |

### Важно: Username - опциональное поле

**Username НЕ обязателен в Telegram:**

❌ **Почему username может быть `None`:**
1. Пользователь не создал @username
2. Пользователь удалил свой @username
3. Пользователь изменил privacy settings

✅ **Что делает код:**
- Если username есть → создает ссылку `https://t.me/{username}`
- Если username нет → показывает "(нет username)"

---

## Тестирование

### Сценарий 1: Кандидат С @username

```bash
# 1. Кандидат (с @ivan_petrov) отправляет /start
# 2. Проверяем лог:
tail -f bot.log | grep "User started interview"

# Вывод:
{
  "message": "User started interview",
  "user_id": 123456789,
  "username": "ivan_petrov",  ← ✅ Автоматически получен!
  "first_name": "Иван",
  "last_name": "Петров"
}

# 3. Проверяем БД:
sqlite3 recruitsmart.db "SELECT telegram_id, username FROM users WHERE telegram_id = 123456789;"

# Вывод:
123456789|ivan_petrov  ← ✅ Сохранен автоматически!

# 4. Рекрутер открывает профиль в админке
# 5. Видит кнопку: [Открыть чат] → https://t.me/ivan_petrov
# 6. Клик → Telegram открывает личный чат ✅
```

### Сценарий 2: Кандидат БЕЗ @username

```bash
# 1. Кандидат (без @username) отправляет /start
# 2. Проверяем лог:
tail -f bot.log | grep "User started interview"

# Вывод:
{
  "message": "User started interview",
  "user_id": 987654321,
  "username": null,  ← ⚠️ У пользователя нет @username
  "first_name": "Мария",
  "last_name": "Иванова"
}

# 3. Проверяем БД:
sqlite3 recruitsmart.db "SELECT telegram_id, username FROM users WHERE telegram_id = 987654321;"

# Вывод:
987654321|  ← username = NULL

# 4. Рекрутер открывает профиль в админке
# 5. Видит: Telegram ID: 987654321 (нет username)
# 6. Кнопка "Открыть чат" не отображается ⚠️
```

---

## FAQ

### Q: Нужно ли просить кандидата ввести username?

**A:** ❌ **НЕТ!** Username парсится **автоматически** из Telegram API.

### Q: Что если у кандидата нет @username?

**A:** Это ограничение Telegram. Невозможно создать direct link без username. В админке показывается "(нет username)".

**Решение:** Можно добавить в бота подсказку:
```
"💡 Совет: Создайте @username в Telegram для быстрой связи с рекрутером:
Настройки → Редактировать профиль → Имя пользователя"
```

### Q: Обновляется ли username, если кандидат его изменит?

**A:** ✅ **ДА!** При каждом сообщении боту Telegram отправляет актуальные данные:

```python
# При КАЖДОМ взаимодействии:
if username is not None:
    user.username = username  # ✅ Обновляем
```

### Q: Можно ли получить username старых кандидатов?

**A:** Да, через Telegram Bot API:

```python
async def backfill_usernames():
    """Обновить username всех пользователей через Bot API."""
    bot = get_bot()
    users = await get_all_users_without_username()

    for user in users:
        try:
            # ✅ Bot API предоставляет username
            tg_chat = await bot.get_chat(user.telegram_id)
            if tg_chat.username:
                await update_user_username(user.id, tg_chat.username)
        except Exception as e:
            logger.warning(f"Cannot get username for {user.telegram_id}: {e}")
```

### Q: Безопасно ли хранить username в БД?

**A:** ✅ **ДА!** Username - публичная информация в Telegram. Любой может найти пользователя по @username через поиск.

### Q: Что если кандидат заблокировал бота?

**A:** Ссылка `https://t.me/{username}` все равно откроет чат. Рекрутер сможет написать, но кандидат не получит сообщение (из-за блокировки).

---

## Альтернативные способы (НЕ рекомендуется)

### ❌ Вариант 1: Просить ввести вручную

```python
# Плохо: Лишний шаг для пользователя
await bot.send_message(user_id, "Введите ваш @username:")
# ... wait for input ...
# Проблемы:
# - Пользователь может ошибиться
# - Дополнительный friction
# - Telegram УЖЕ предоставляет эту информацию!
```

### ❌ Вариант 2: Использовать tg://user?id={id}

```python
# Плохо: Открывает профиль, но НЕ начинает чат
<a href="tg://user?id={{ user.telegram_id }}">Открыть чат</a>
# Не работает из браузера
# Открывает профиль вместо чата
```

### ✅ Правильный вариант: Автоматический парсинг (реализован)

```python
# Хорошо: Telegram сам предоставляет username
username = message.from_user.username  # ✅ Автоматически!
```

---

## Выводы

### ✅ Что реализовано

1. **Автоматический парсинг username** из Telegram API
2. **Сохранение в state** при первом контакте
3. **Запись в БД** после прохождения Test 1
4. **Обновление при каждом взаимодействии**
5. **Правильная ссылка в админке** (`https://t.me/{username}`)
6. **Обработка случая отсутствия username**
7. **Логирование для отладки**

### ✅ Преимущества

- **Нет ручного ввода** - кандидат ничего не вводит
- **Нет ошибок** - Telegram гарантирует корректность
- **Автоматическое обновление** - при изменении username
- **Работает из коробки** - не требует настройки

### ⚠️ Ограничения

- **Username опционален** - не все пользователи его имеют
- **Нельзя создать link по ID** - это ограничение Telegram
- **Требует взаимодействие** - username обновляется при сообщении боту

---

**Итог:** Username парсится **АВТОМАТИЧЕСКИ**, кандидату **НЕ нужно ничего вводить**! 🎉
