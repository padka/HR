# 🔄 Инструкция: Обновление дашборда после изменений

## ⚠️ Проблема

После интеграции с БД вы видите старые хардкод-данные:
- Рекрутёров показывает 10 вместо реальных 1
- Городов показывает 100 вместо реальных 10

## ✅ Причина

Браузер закэшировал старую версию HTML (до интеграции с БД).

## 🔧 Решение

### Вариант 1: Hard Reload (Быстро)

#### На Mac:
```
Cmd + Shift + R
```

#### На Windows/Linux:
```
Ctrl + Shift + R
```

#### Альтернативно:
1. Открыть DevTools (F12)
2. Правый клик на кнопке обновления браузера
3. Выбрать "Empty Cache and Hard Reload"

---

### Вариант 2: Очистить кэш браузера

#### Chrome:
1. Открыть: `chrome://settings/clearBrowserData`
2. Выбрать "Cached images and files"
3. Time range: "All time"
4. Нажать "Clear data"

#### Firefox:
1. Открыть: `about:preferences#privacy`
2. Найти "Cookies and Site Data"
3. Нажать "Clear Data"
4. Выбрать "Cached Web Content"

#### Safari:
1. Safari → Preferences → Advanced
2. Включить "Show Develop menu"
3. Develop → Empty Caches

---

### Вариант 3: Режим инкогнито

1. Открыть новое окно инкогнито:
   - Chrome/Edge: `Ctrl + Shift + N` (Win) или `Cmd + Shift + N` (Mac)
   - Firefox: `Ctrl + Shift + P` (Win) или `Cmd + Shift + P` (Mac)
   - Safari: `Cmd + Shift + N`

2. Перейти на `http://localhost:8000/`

---

## 🧪 Проверка после очистки кэша

После hard reload откройте DevTools (F12) → Console и проверьте:

```javascript
// Проверить значения в HTML
document.querySelector('[data-count-value]').textContent
// Должно показать "1" для рекрутёров

document.querySelectorAll('[data-count-value]').forEach(el => {
  console.log(el.previousElementSibling.textContent, ':', el.textContent);
});
// Вывод:
// Рекрутёры : 1
// Города : 10
// Слоты всего : 11
```

---

## 📊 Реальные данные из БД

После очистки кэша вы должны видеть:

### KPI Metrics
- **Рекрутёры**: 1 (из БД)
- **Города**: 10 (из БД)
- **Слоты всего**: 11 (из БД)
  - FREE: 11
  - PENDING: 0
  - BOOKED: 0

### Recent Applications
Таблица будет показывать реальных кандидатов из таблицы `users` (если есть), или:
```
Нет данных о кандидатах
```

### Upcoming Interviews
Список будет показывать реальные забронированные слоты (если есть на сегодня/завтра), или:
```
Нет запланированных интервью на сегодня
```

---

## 🔍 Отладка (если проблема осталась)

### 1. Проверить, что сервер использует новый код

```bash
# Перезапустить сервер
lsof -ti:8000 | xargs kill -9
.venv/bin/uvicorn backend.apps.admin_ui.app:app --host 127.0.0.1 --port 8000 --reload
```

### 2. Проверить данные напрямую из БД

```bash
.venv/bin/python -c "
import asyncio
from backend.apps.admin_ui.services.dashboard import dashboard_counts

async def test():
    counts = await dashboard_counts()
    print('Recruiters:', counts['recruiters'])
    print('Cities:', counts['cities'])
    print('Slots Total:', counts['slots_total'])

asyncio.run(test())
"
```

### 3. Проверить Network tab в DevTools

1. Открыть DevTools (F12) → Network
2. Hard reload (Cmd+Shift+R / Ctrl+Shift+R)
3. Найти запрос к `/`
4. Проверить Response:
   - Должны быть `{{ counts.recruiters }}` → реальное число
   - НЕ должно быть хардкод-чисел в `<div class="metric-card__value">`

---

## ✅ Успешный результат

После hard reload вы должны видеть:
- ✅ Реальные числа из БД в KPI карточках
- ✅ Анимация счётчика работает (числа наращиваются от 0)
- ✅ 3D tilt эффект работает
- ✅ Neural Network фон анимируется
- ✅ Таблица кандидатов показывает реальные данные или "Нет данных"
- ✅ Список интервью показывает реальные данные или "Нет интервью"

---

## 🚫 Если ничего не помогло

Попробуйте:
1. Открыть в другом браузере
2. Проверить, что файл `index.html` действительно сохранён с изменениями:
   ```bash
   grep -n "counts.recruiters" backend/apps/admin_ui/templates/index.html
   # Должна быть строка с {{ counts.recruiters }}
   ```
3. Проверить логи сервера на наличие ошибок

---

**Дата**: 24 ноября 2025
**Версия**: 2.1.0
