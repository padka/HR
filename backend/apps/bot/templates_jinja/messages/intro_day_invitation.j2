{# Приглашение на ознакомительный день #}
{% import 'blocks/header.j2' as header %}
{% import 'blocks/info_row.j2' as info %}
{% import 'blocks/checklist.j2' as checklist %}
{% import 'blocks/footer_hint.j2' as footer %}

{{ header.render('🎉', 'Ознакомительный день')|safe }}

Вы успешно прошли видеоинтервью в компанию <b>SMART</b>! 🎉

{{ info.date(start_utc, tz_name)|safe }}
{% if address %}
{{ info.location(address)|safe }}
{% endif %}

✨ <b>Что это?</b>
Это не собеседование, а знакомство с командой
и форматом работы (~2 часа).

{{ checklist.render('Что взять с собой', [
    'Паспорт',
    'Блокнот и ручка (опционально)',
    'Отличное настроение'
])|safe }}

{% if contact_name and contact_phone %}
{{ info.contact(contact_name, contact_phone)|safe }}
{% endif %}

{{ footer.render('Приходите в презентабельном виде. Проявите активность — это важно!')|safe }}
