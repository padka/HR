{# Подтверждение записи на собеседование #}
{% import 'blocks/header.j2' as header %}
{% import 'blocks/info_row.j2' as info %}
{% import 'blocks/checklist.j2' as checklist %}
{% import 'blocks/footer_hint.j2' as footer %}

{{ header.render('✅', 'Встреча подтверждена')|safe }}

👋 {{ candidate_name }}, вы шаг ближе к команде SMART!

{{ info.date(start_utc, tz_name)|safe }}
{{ info.format(format_text|default('Видеозвонок • 15-20 мин'))|safe }}

{{ checklist.render('Подготовьтесь заранее', [
    'Стабильный интернет (минимум 5 Мбит/с)',
    'Тихое место для разговора',
    'Наушники с микрофоном (опционально)',
    '2-3 вопроса о вакансии'
])|safe }}

{{ footer.render('🔔 Поставьте напоминание на телефон. Ссылка придёт за 2 часа до встречи.')|safe }}
